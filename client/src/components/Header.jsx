// src/components/Header.jsx

const Header = () => {
    return (
        <header className="custom-header">
            <h1 className="custom-title">KeebsForSpeed</h1>
        </header>
    );
};

export default Header;
